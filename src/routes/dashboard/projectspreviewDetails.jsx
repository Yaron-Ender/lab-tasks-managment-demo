import React from 'react';

const ProjectspreviewDetails = ({x}) => {
console.log(x)
    return (
        <div>
          <h2>{x.monographName}</h2>  
        </div>
    );
};

export default ProjectspreviewDetails;